# Offline ML pipeline dependencies (data prep, training, evaluation, artifact export).
# Pin major versions for reproducibility; exact patch versions aren't pinned so
# security fixes still flow in, but the API surfaces this code relies on are stable.
pandas>=2.2,<3
numpy>=1.26,<2
scipy>=1.12,<2
scikit-learn>=1.4,<2
implicit>=0.7,<0.8
scikit-surprise>=1.1,<1.2
sentence-transformers>=3.0,<4
torch>=2.2,<3
pyarrow>=15.0
requests>=2.31
joblib>=1.3
faiss-cpu>=1.8,<2

pytest>=8.0
