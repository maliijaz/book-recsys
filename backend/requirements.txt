# Lean serving-only dependencies -- deliberately excludes torch, sentence-transformers,
# surprise, implicit: the backend only does numpy/pandas lookups against artifacts the
# offline pipeline already produced, never model inference or training.
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pandas>=2.2,<3
numpy>=1.26,<2
pyarrow>=15.0
pydantic>=2.7,<3
